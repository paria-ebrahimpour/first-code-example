import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2a9d8f",
      dark: "#264653",
    },
    secomdary: {
      main: "#f4a261",
      dark: "#e9c46a",
    },
    error: {
      main: "#e76f51",
    },
  },
});
export default theme;
